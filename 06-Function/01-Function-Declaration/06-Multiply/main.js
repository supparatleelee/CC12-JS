// function multiply(x, y) {
//     let z = x*y;
//     return z;
// }

// multiply(3, 2);

function multiply(x, y) {
    return x*y;
    //return retuens value that we can further use it later.
}

multiply(3, 2);
let result = multiply(10, 2.5)
console.log(multiply(2, 3)) // log function value
console.log(multiply) // log function name