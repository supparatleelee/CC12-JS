function squareNum(num) {
    const result = num * num;
  }
  
  console.log(squareNum(2)); // * // undefined because there is no return from the function.
  //if didn't return, you can't use that value further.
  //return result -> return 4.